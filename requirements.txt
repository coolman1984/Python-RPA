PySide6==6.11.2
playwright==1.62.0
openpyxl==3.1.5
PyYAML==6.0.3
PyInstaller==6.22.2
pytest>=9.0

# Windows-native discovery. Imported lazily, so the package installs and the tests run anywhere,
# but a Windows build without these has no desktop layer at all.
pywin32>=311; sys_platform == "win32"
pywinauto>=0.6.9; sys_platform == "win32"
comtypes>=1.4.0; sys_platform == "win32"
pynput>=1.7.7; sys_platform == "win32"
Pillow>=10.0.0

# Optional extras, deliberately not bundled: they are large, and neither layer is implemented
# enough to earn the size yet.
#   pytesseract  + a Tesseract install  -> the OCR layer
#   opencv-python                       -> a real visual matcher
